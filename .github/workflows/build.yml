name: Build Narzo 50 Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential bc bison flex libssl-dev libncurses5-dev libelf-dev git curl python3 zip

    - name: Setup Neutron Clang
      run: |
        mkdir -p clang
        cd clang
        curl -LO "https://raw.githubusercontent.com/Neutron-Toolchains/antman/main/antman"
        chmod +x antman
        ./antman -S
        
    - name: Clone Kernel Source
      run: |
        git clone --depth=1 https://github.com/realme-mt6781-dev/android_kernel_realme_mt6781.git kernel_source

    - name: Setup KernelSU-Next & SUSFS
      run: |
        cd kernel_source
        
        echo "--- Installing KernelSU-Next ---"
        curl -LSs "https://raw.githubusercontent.com/rifsxd/KernelSU-Next/next/kernel/setup.sh" | bash -
        
        echo "--- Downloading SUSFS ---"
        git clone https://gitlab.com/simonpunk/susfs4ksu.git -b kernel-4.19
        
        echo "--- Patching SUSFS (Auto-Find Mode) ---"
        # ফোল্ডার খোঁজার স্মার্ট কমান্ড
        # এটা susfs4ksu এর ভেতর যেখানেই 'susfs' ফোল্ডার পাবে, সেখান থেকে কপি করবে
        SUSFS_SRC=$(find susfs4ksu -type d -name "susfs" | grep "fs/susfs" | head -n 1)
        
        if [ -z "$SUSFS_SRC" ]; then
          echo "Error: SUSFS source folder not found! Listing files for debug:"
          ls -R susfs4ksu
          exit 1
        fi
        
        echo "Found SUSFS source at: $SUSFS_SRC"
        cp -r "$SUSFS_SRC" fs/
        
        # Header ফাইল খোঁজার স্মার্ট কমান্ড
        SUSFS_HEADER=$(find susfs4ksu -type f -name "susfs.h" | head -n 1)
        
        if [ -z "$SUSFS_HEADER" ]; then
           echo "Error: SUSFS header not found!"
           exit 1
        fi
        
        echo "Found SUSFS header at: $SUSFS_HEADER"
        cp "$SUSFS_HEADER" include/linux/

        # Config ফাইল এডিট করা
        sed -i '/source "fs\/notify\/Kconfig"/a source "fs/susfs/Kconfig"' fs/Kconfig
        sed -i '/obj-$(CONFIG_QUOTA)/a obj-$(CONFIG_SUSFS) += susfs/' fs/Makefile

    - name: Build Kernel
      run: |
        cd kernel_source
        export PATH=$GITHUB_WORKSPACE/clang/bin:$PATH
        export ARCH=arm64
        export SUBARCH=arm64
        export KBUILD_BUILD_USER="kafi5"
        export KBUILD_BUILD_HOST="GitHub-Action"
        
        make O=out clean mrproper
        make O=out spaced_defconfig
        
        echo "CONFIG_KSU=y" >> out/.config
        echo "CONFIG_KSU_SUSFS=y" >> out/.config
        echo "CONFIG_SUSFS=y" >> out/.config
        echo "CONFIG_SUSFS_ENABLE_LOG=y" >> out/.config
        
        make O=out olddefconfig

        make -j$(nproc --all) O=out \
                              CC=clang \
                              LD=ld.lld \
                              AR=llvm-ar \
                              NM=llvm-nm \
                              OBJCOPY=llvm-objcopy \
                              OBJDUMP=llvm-objdump \
                              STRIP=llvm-strip \
                              CROSS_COMPILE=aarch64-linux-gnu- \
                              CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
                              Image.gz-dtb

    - name: Create Flashable Zip (AnyKernel3)
      run: |
        git clone https://github.com/osm0sis/AnyKernel3.git
        cp kernel_source/out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
        cd AnyKernel3
        zip -r9 ../Narzo50-Kernel-KSU-SUSFS.zip * -x .git README.md *placeholder

    - name: Upload Kernel Zip
      uses: actions/upload-artifact@v4
      with:
        name: Narzo50-Kernel-Zip
        path: Narzo50-Kernel-KSU-SUSFS.zip
